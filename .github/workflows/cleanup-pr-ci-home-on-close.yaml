name: Cleanup PR CI Home On Close

on:
  pull_request:
    types: [closed]

permissions:
  contents: read

jobs:
  cleanup-pr-ci-home:
    name: cleanup - PR CI home on close
    runs-on: [self-hosted, h100]
    timeout-minutes: 5
    container:
      image: crpi-n6adu6llixz83q37.cn-hangzhou.personal.cr.aliyuncs.com/hongccc/sglang-omni:dev
      options: --rm -e NVIDIA_VISIBLE_DEVICES=6,7 -v /dev/shm:/dev/shm
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Remove PR CI home
        shell: bash
        run: |
          bash .github/scripts/cleanup_ci_pr_home.sh \
            "/github/home/pr-${{ github.event.pull_request.number }}"
