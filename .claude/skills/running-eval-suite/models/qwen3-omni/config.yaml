# running-eval-suite config — every reference table row across the
# 6 benchmarks/eval/benchmark_*.py files is described here.
#
# `running-eval-suite` reads each row, runs the server + client, then
# either replaces the matching row in the benchmark .py file (same
# hardware tag) or appends a new row at the end of that section's
# table (new hardware). "Local v1 Pipeline Result" sections are
# always skipped — those are contributors' personal experiments, not
# refresh targets.
#
# Schema (per row):
#   id                          handle (informational; row selection is by --benchmarks short name)
#   file                        path (relative to repo root) of the benchmark .py
#   hf_model_id                 (optional) overrides the config-level default; needed for s2-pro rows
#   locate.section_substring    substring of the section header above the table (e.g. "Accuracy (accuracy)")
#   locate.config_substring     substring of the row's Config cell (e.g. "modalities=text ")
#   locate.source_workload      workload tag WITHOUT the [hardware,] prefix; e.g. "V1-pipeline, full-set, c=8".
#                               At runtime the skill prepends the detected hw tag → "[H200, V1-pipeline, full-set, c=8]"
#   server                      server launch template; {port} {model} {gpus} substituted
#   server_gpus                 1 (text-only) or 2 (full-pipeline talker)
#   server_health               health URL ({port} substituted)
#   server_boot_timeout_s       seconds to wait for /health 2xx
#   client                      client launch template; {port} {output_dir} {model} substituted
#   result_json                 path under output_dir where the client writes its JSON
#   cells                       column header → { path, format } OR { paths: { alias: ... }, format }

name: qwen3-omni
description: "Reference benchmarks for Qwen3-Omni 30B-A3B-Instruct + S2-Pro TTS"

# Default model ID; per-row hf_model_id overrides this (s2-pro rows do).
hf_model_id: "Qwen/Qwen3-Omni-30B-A3B-Instruct"

hf_datasets:
  - "ddwang2000/MMSU"
  - "MMMU/MMMU"
  - "zhaochenyang20/Video_MME"
  - "zhaochenyang20/Video_AMME_ci"
  - "zhaochenyang20/Video_MME_ci"
  - "zhaochenyang20/seed-tts-eval"

default_venv_python:
  - "/sgl-workspace/sglang-omni/omni-qwen3/bin/python"
  - "/github/home/omni-qwen3/bin/python"
  - "/data/sglang-omni/omni-qwen3/bin/python"

# FLASHINFER_DISABLE_VERSION_CHECK silences the flashinfer / flashinfer-jit-cache
# version mismatch raise on dev images where the JIT cache is pinned to an older
# minor (verified on frankleeeee/sglang-omni:dev 2026-05-05: jit-cache 0.5.3 vs
# flashinfer 0.6.1). The mismatch is benign on these images.
auto_env:
  FLASHINFER_DISABLE_VERSION_CHECK: "1"
  HF_ENDPOINT: "https://hf-mirror.com"
  SGLANG_OMNI_SERVER_VERSION: "v1"

rows:
  # ===================== mmsu (Qwen3-Omni text-only) =====================
  - id: mmsu-text-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    locate:
      section_substring: "Accuracy (accuracy)"
      config_substring: "modalities=text "
      source_workload: "V1-pipeline, full-set, c=8"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text --max-concurrency 8 --output-dir {output_dir}"
    result_json: "mmsu_results.json"
    cells:
      overall_accuracy:    { path: "summary.overall_accuracy", format: "{v:.2%}" }
      parseable_samples:   { paths: { v: "summary.parseable_samples", t: "summary.total_samples" }, format: "{v}/{t}" }
      unparseable_samples: { path: "summary.unparseable_samples", format: "{v}" }

  - id: mmsu-text-audio-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    locate:
      section_substring: "Accuracy (accuracy)"
      config_substring: "modalities=text+audio"
      source_workload: "V1-pipeline, full-set, c=8"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text+audio --max-concurrency 8 --output-dir {output_dir}"
    result_json: "mmsu_results.json"
    cells:
      overall_accuracy:    { path: "summary.overall_accuracy", format: "{v:.2%}" }
      parseable_samples:   { paths: { v: "summary.parseable_samples", t: "summary.total_samples" }, format: "{v}/{t}" }
      unparseable_samples: { path: "summary.unparseable_samples", format: "{v}" }

  - id: mmsu-text-c8-speed
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    locate:
      section_substring: "Speed (speed)"
      config_substring: "modalities=text "
      source_workload: "V1-pipeline, full-set, c=8"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text --max-concurrency 8 --output-dir {output_dir}"
    result_json: "mmsu_results.json"
    cells:
      latency_mean_s: { path: "speed_metrics.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed_metrics.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed_metrics.throughput_qps", format: "{v:.2f}" }
      tok_per_s_mean: { path: "speed_metrics.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed_metrics.tok_per_s_agg",  format: "{v:.1f}" }

  - id: mmsu-text-audio-c8-speed
    file: "benchmarks/eval/benchmark_omni_mmsu.py"
    locate:
      section_substring: "Speed (speed)"
      config_substring: "modalities=text+audio"
      source_workload: "V1-pipeline, full-set, c=8, text-only server"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python benchmarks/eval/benchmark_omni_mmsu.py --model qwen3-omni --port {port} --modalities text+audio --max-concurrency 8 --output-dir {output_dir}"
    result_json: "mmsu_results.json"
    cells:
      latency_mean_s: { path: "speed_metrics.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed_metrics.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed_metrics.throughput_qps", format: "{v:.2f}" }
      tok_per_s_mean: { path: "speed_metrics.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed_metrics.tok_per_s_agg",  format: "{v:.1f}" }

  # ===================== mmmu (Qwen3-Omni text-only c=8 + full-pipeline c=1) =====================
  - id: mmmu-no-audio-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Accuracy (summary)"
      config_substring: "enable_audio=False"
      source_workload: "V1-pipeline, full-set, c=8, max_tokens=2048"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 2048 --output-dir {output_dir}"
    result_json: "mmmu_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: mmmu-audio-c1-accuracy
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Accuracy (summary)"
      config_substring: "enable_audio=True"
      source_workload: "V1-pipeline, 50-sample subset, c=1, max_tokens=2048"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 1 --max-tokens 2048 --enable-audio --max-samples 50 --output-dir {output_dir}"
    result_json: "mmmu_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: mmmu-no-audio-c8-speed
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Speed (speed)"
      config_substring: "enable_audio=False"
      source_workload: "V1-pipeline, full-set, c=8, max_tokens=2048"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 2048 --output-dir {output_dir}"
    result_json: "mmmu_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  - id: mmmu-audio-c1-speed
    file: "benchmarks/eval/benchmark_omni_mmmu.py"
    locate:
      section_substring: "Speed (speed)"
      config_substring: "enable_audio=True"
      source_workload: "V1-pipeline, **50-sample subset**, c=1, max_tokens=2048"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python benchmarks/eval/benchmark_omni_mmmu.py --model qwen3-omni --port {port} --max-concurrency 1 --max-tokens 2048 --enable-audio --max-samples 50 --output-dir {output_dir}"
    result_json: "mmmu_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  # ===================== seedtts (Qwen3-Omni full-pipeline talker, c=16) =====================
  # NOTE: the EN VC=T accuracy row's reference Source carries `n=3 mean`. If
  # you run --rounds 1 here the cell values reflect a single round; the
  # locator still matches but the resulting reference is no longer a 3-round
  # mean. Run --rounds 3 and manually average if you need a true n=3 mean
  # (multi-round aggregation isn't built in yet).
  - id: seedtts-en-vc-t-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, voice_clone=T"
      source_workload: "V1-pipeline, full-set, c=16, n=3 mean"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-en-vc-f-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, voice_clone=F"
      source_workload: "V1-pipeline, full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone false --model qwen3-omni --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-zh-vc-t-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, voice_clone=T"
      source_workload: "V1-pipeline, full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-zh-vc-f-c16-accuracy
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, voice_clone=F"
      source_workload: "V1-pipeline, full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone false --model qwen3-omni --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: seedtts-en-vc-t-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, voice_clone=T"
      source_workload: "V1-pipeline, full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  - id: seedtts-en-vc-f-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, voice_clone=F"
      source_workload: "V1-pipeline, full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone false --model qwen3-omni --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  - id: seedtts-zh-vc-t-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, voice_clone=T"
      source_workload: "V1-pipeline, full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --model qwen3-omni --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  - id: seedtts-zh-vc-f-c16-speed
    file: "benchmarks/eval/benchmark_omni_seedtts.py"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, voice_clone=F"
      source_workload: "V1-pipeline, full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_seedtts --voice-clone false --model qwen3-omni --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.2f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.2f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.2f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  # ===================== videomme (Qwen3-Omni text-only c=4, 2520 samples) =====================
  - id: videomme-thinker-only-c4-accuracy
    file: "benchmarks/eval/benchmark_omni_videomme.py"
    locate:
      section_substring: "Accuracy (full set)"
      config_substring: "thinker-only, full-set, c=4"
      source_workload: "c=4, max_tokens=256"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_omni_videomme --model qwen3-omni --port {port} --max-concurrency 4 --max-tokens 256 --video-fps 2 --video-max-frames 128 --video-max-pixels 401408 --output-dir {output_dir}"
    result_json: "videomme_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: videomme-thinker-only-c4-speed
    file: "benchmarks/eval/benchmark_omni_videomme.py"
    locate:
      section_substring: "Speed (full set)"
      config_substring: "thinker-only, full-set, c=4"
      source_workload: "c=4, max_tokens=256"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_omni_videomme --model qwen3-omni --port {port} --max-concurrency 4 --max-tokens 256 --video-fps 2 --video-max-frames 128 --video-max-pixels 401408 --output-dir {output_dir}"
    result_json: "videomme_results.json"
    cells:
      completed:           { path: "speed.completed_requests", format: "{v}" }
      failed:              { path: "speed.failed_requests", format: "{v}" }
      latency_mean_s:      { path: "speed.latency_mean_s",   format: "{v:.3f}" }
      latency_median_s:    { path: "speed.latency_median_s", format: "{v:.3f}" }
      latency_p95_s:       { path: "speed.latency_p95_s",    format: "{v:.3f}" }
      latency_p99_s:       { path: "speed.latency_p99_s",    format: "{v:.3f}" }
      tok_per_s_mean:      { path: "speed.tok_per_s_mean",   format: "{v:.1f}" }
      tok_per_s_agg:       { path: "speed.tok_per_s_agg",    format: "{v:.1f}" }
      gen_tokens_mean:     { path: "speed.gen_tokens_mean",  format: "{v}" }
      gen_tokens_total:    { path: "speed.gen_tokens_total", format: "{v}" }
      prompt_tokens_mean:  { path: "speed.prompt_tokens_mean",  format: "{v}" }
      prompt_tokens_total: { path: "speed.prompt_tokens_total", format: "{v}" }
      throughput_qps:      { path: "speed.throughput_qps",   format: "{v:.3f}" }

  # ===================== videoamme (Qwen3-Omni; ci-50 thinker-only + ci-10 thinker-talker) =====================
  - id: videoamme-thinker-only-ci50-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    locate:
      section_substring: "Accuracy"
      config_substring: "thinker-only, ci-50, c=8"
      source_workload: "c=8, max_tokens=256"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 256 --output-dir {output_dir}"
    result_json: "videoamme_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: videoamme-thinker-talker-ci10-c8-accuracy
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    locate:
      section_substring: "Accuracy"
      config_substring: "thinker-talker, ci-10, c=8"
      source_workload: "c=8, max_tokens=256"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --enable-audio --max-concurrency 8 --max-tokens 256 --output-dir {output_dir}"
    result_json: "videoamme_results.json"
    cells:
      accuracy:    { path: "summary.accuracy", format: "{v:.2%}" }
      correct:     { paths: { v: "summary.correct", t: "summary.total_samples" }, format: "{v}/{t}" }
      failed:      { path: "summary.failed", format: "{v}" }
      mc_fallback: { path: "summary.mc_fallback", format: "{v}" }

  - id: videoamme-thinker-only-ci50-c8-speed
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    locate:
      section_substring: "Speed"
      config_substring: "thinker-only, ci-50, c=8"
      source_workload: "c=8, max_tokens=256"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --text-only --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --max-concurrency 8 --max-tokens 256 --output-dir {output_dir}"
    result_json: "videoamme_results.json"
    cells:
      completed:           { path: "speed.completed_requests", format: "{v}" }
      failed:              { path: "speed.failed_requests", format: "{v}" }
      latency_mean_s:      { path: "speed.latency_mean_s",   format: "{v:.3f}" }
      latency_median_s:    { path: "speed.latency_median_s", format: "{v:.3f}" }
      latency_p95_s:       { path: "speed.latency_p95_s",    format: "{v:.3f}" }
      latency_p99_s:       { path: "speed.latency_p99_s",    format: "{v:.3f}" }
      tok_per_s_mean:      { path: "speed.tok_per_s_mean",   format: "{v:.1f}" }
      tok_per_s_agg:       { path: "speed.tok_per_s_agg",    format: "{v:.1f}" }
      gen_tokens_mean:     { path: "speed.gen_tokens_mean",  format: "{v}" }
      gen_tokens_total:    { path: "speed.gen_tokens_total", format: "{v}" }
      prompt_tokens_mean:  { path: "speed.prompt_tokens_mean",  format: "{v}" }
      prompt_tokens_total: { path: "speed.prompt_tokens_total", format: "{v}" }
      throughput_qps:      { path: "speed.throughput_qps",   format: "{v:.3f}" }

  - id: videoamme-thinker-talker-ci10-c8-speed
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    locate:
      section_substring: "Speed"
      config_substring: "thinker-talker, ci-10, c=8"
      source_workload: "c=8, max_tokens=256"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --enable-audio --max-concurrency 8 --max-tokens 256 --output-dir {output_dir}"
    result_json: "videoamme_results.json"
    cells:
      completed:           { path: "speed.completed_requests", format: "{v}" }
      failed:              { path: "speed.failed_requests", format: "{v}" }
      latency_mean_s:      { path: "speed.latency_mean_s",   format: "{v:.3f}" }
      latency_median_s:    { path: "speed.latency_median_s", format: "{v:.3f}" }
      latency_p95_s:       { path: "speed.latency_p95_s",    format: "{v:.3f}" }
      latency_p99_s:       { path: "speed.latency_p99_s",    format: "{v:.3f}" }
      tok_per_s_mean:      { path: "speed.tok_per_s_mean",   format: "{v:.1f}" }
      tok_per_s_agg:       { path: "speed.tok_per_s_agg",    format: "{v:.1f}" }
      gen_tokens_mean:     { path: "speed.gen_tokens_mean",  format: "{v}" }
      gen_tokens_total:    { path: "speed.gen_tokens_total", format: "{v}" }
      prompt_tokens_mean:  { path: "speed.prompt_tokens_mean",  format: "{v}" }
      prompt_tokens_total: { path: "speed.prompt_tokens_total", format: "{v}" }
      throughput_qps:      { path: "speed.throughput_qps",   format: "{v:.3f}" }

  - id: videoamme-thinker-talker-ci10-c8-wer
    file: "benchmarks/eval/benchmark_omni_videoamme.py"
    locate:
      section_substring: "Talker WER"
      config_substring: "thinker-talker, ci-10, c=8"
      source_workload: "c=8, max_tokens=256"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --talker-cuda-graph on --port {port}"
    server_gpus: 2
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 600
    client: "python -m benchmarks.eval.benchmark_omni_videoamme --model qwen3-omni --port {port} --enable-audio --max-concurrency 8 --max-tokens 256 --output-dir {output_dir}"
    result_json: "videoamme_results.json"
    cells:
      evaluated:             { paths: { v: "wer.summary.evaluated", t: "wer.summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "wer.summary.skipped", format: "{v}" }
      wer_corpus:            { path: "wer.summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "wer.summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_p95:    { path: "wer.summary.wer_per_sample_p95",    format: "{v:.2%}" }
      wer_per_sample_max:    { path: "wer.summary.wer_per_sample_max",    format: "{v:.2%}" }
      n_above_50_pct_wer:    { path: "wer.summary.n_above_50_pct_wer",    format: "{v}" }
      rtf_mean:              { path: "wer.summary.rtf_mean",              format: "{v:.4f}" }
      audio_duration_mean_s: { path: "wer.summary.audio_duration_mean_s", format: "{v:.3f}" }

  # ===================== s2-pro tts (separate model, fishaudio/s2-pro) =====================
  - id: s2pro-tts-en-stream-false-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, stream=False"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-en-stream-true-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "EN, stream=True"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-zh-stream-false-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, stream=False"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-zh-stream-true-c16-accuracy
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Accuracy (accuracy.wer)"
      config_substring: "ZH, stream=True"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "wer_results.json"
    cells:
      wer_corpus:            { path: "summary.wer_corpus",            format: "{v:.2%}" }
      wer_per_sample_mean:   { path: "summary.wer_per_sample_mean",   format: "{v:.2%}" }
      wer_per_sample_median: { path: "summary.wer_per_sample_median", format: "{v:.2%}" }
      wer_per_sample_std:    { path: "summary.wer_per_sample_std",    format: "{v:.1%}" }
      evaluated:             { paths: { v: "summary.evaluated", t: "summary.total" }, format: "{v}/{t}" }
      skipped:               { path: "summary.skipped", format: "{v}" }

  - id: s2pro-tts-en-stream-false-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, stream=False"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  - id: s2pro-tts-en-stream-true-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "EN, stream=True"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta seedtts_testset/en/meta.lst --max-concurrency 16 --lang en --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  - id: s2pro-tts-zh-stream-false-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, stream=False"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --model fishaudio/s2-pro --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }

  - id: s2pro-tts-zh-stream-true-c16-speed
    file: "benchmarks/eval/benchmark_tts_seedtts.py"
    hf_model_id: "fishaudio/s2-pro"
    locate:
      section_substring: "Generation speed (generation.speed)"
      config_substring: "ZH, stream=True"
      source_workload: "full-set, c=16"
    server: "python -m sglang_omni.cli serve --model-path {model} --version v1 --port {port}"
    server_gpus: 1
    server_health: "http://localhost:{port}/health"
    server_boot_timeout_s: 300
    client: "python -m benchmarks.eval.benchmark_tts_seedtts --stream --model fishaudio/s2-pro --port {port} --meta seedtts_testset/zh/meta.lst --max-concurrency 16 --lang zh --output-dir {output_dir}"
    result_json: "speed_results.json"
    cells:
      latency_mean_s: { path: "speed.latency_mean_s", format: "{v:.3f}" }
      latency_p95_s:  { path: "speed.latency_p95_s",  format: "{v:.3f}" }
      rtf_mean:       { path: "speed.rtf_mean",       format: "{v:.3f}" }
      throughput_qps: { path: "speed.throughput_qps", format: "{v:.3f}" }
      tok_per_s_mean: { path: "speed.tok_per_s_mean", format: "{v:.1f}" }
      tok_per_s_agg:  { path: "speed.tok_per_s_agg",  format: "{v:.1f}" }
